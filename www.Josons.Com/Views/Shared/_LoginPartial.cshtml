@if(Request.IsAuthenticated) {
    
    @Html.ActionLink(User.Identity.Name, "Index", "Account", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
else {
    
    @Html.ActionLink("Log in", "Index", "SSOLogin", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}

  @Html.ActionLink("Log Out", "Index", "SSOLogOut", routeValues: null, htmlAttributes: new { data_icon = "gear" })